-- displays all records of the table second_table
-- displays in the order (score and name)
-- records are ordered by the top score
SELECT score, name FROM second_table ORDER BY score DESC, name DESC;
